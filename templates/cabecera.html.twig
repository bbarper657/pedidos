{% if app.user %}

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/cabecera.css') }}">
    {% endblock %}
    <header class="header">
        <div class="logo">MiTienda</div>
        <div class="saludo">
            Hola, <strong>{{ app.user.login }}</strong>
        </div>
        <nav>
            <a href="{{ path('categorias') }}">Categorías</a>
            <a href="{{ path('cesta') }}">Cesta</a>
            <a href="{{ path('historial') }}">Historial de Pedidos</a>
            <a href="{{ path('logout') }}">Salir</a>
        </nav>
    </header>
{% endif %}